# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Date custom scalar type"""
scalar Date

type Forum {
  id: ID!
  name: String!
  threads: [Thread!]!
}

type Mutation {
  addForum(newForumData: NewForumInput!): Forum!
  addThread(newThreadData: NewThreadInput!): Thread!
  addReply(newReplyData: NewReplyInput!): Reply!
  addUser(newUserData: NewUserInput!): User!
}

input NewForumInput {
  name: String!
}

input NewReplyInput {
  name: String!
  email: String!
  password: String!
}

input NewThreadInput {
  name: String!
  body: String!
  authorid: Int!
  forumid: Int!
}

input NewUserInput {
  name: String!
  email: String!
  password: String!
}

type Query {
  forum(id: String!): Forum!
  forums(skip: Int = 0, take: Int = 25): [Forum!]!
  thread(id: String!): Thread!
  threads(forumId: String!, skip: Int = 0, take: Int = 25): [Thread!]!
  reply(id: String!): Reply!
  replys(threadId: String!, skip: Int = 0, take: Int = 25): [Reply!]!
  user(id: String!): User!
  users(skip: Int = 0, take: Int = 25): [User!]!
}

type Reply {
  id: ID!
  when: Date!
  content: String!
  thread: Thread!
  author: User!
}

type Subscription {
  forumAdded: Forum!
  threadAdded: Thread!
  replyAdded: Reply!
  userAdded: User!
}

type Thread {
  id: ID!
  when: Date!
  title: String!
  content: String!
  forum: Forum!
  author: User!
  replies: [Reply!]!
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
}
