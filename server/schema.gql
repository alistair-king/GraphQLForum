# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Date custom scalar type"""
scalar Date

type Forum {
  id: ID!
  name: String!
  description: String!
  threads(take: Int!, skip: Int!): PaginatedThreads!
}

type LastReply {
  reply: Reply
  count: Int!
}

type Mutation {
  addForum(newForumData: NewForumInput!): Forum!
  addThread(newThreadData: NewThreadInput!): Thread!
  addUser(newUserData: NewUserInput!): User!
  addReply(newReplyData: NewReplyInput!): Reply!
}

input NewForumInput {
  name: String!
}

input NewReplyInput {
  threadId: String!
  authorId: String!
  content: String!
}

input NewThreadInput {
  name: String!
  body: String!
  authorid: Int!
  forumid: Int!
}

input NewUserInput {
  name: String!
  email: String!
  password: String!
}

type PaginatedReplies {
  items: [Reply!]!
  count: Int!
}

type PaginatedThreads {
  items: [Thread!]!
  count: Int!
}

type Query {
  forum(id: String!): Forum!
  forums(skip: Int = 0, take: Int = 25): [Forum!]!
  thread(id: String!): Thread!
  user(id: String!): User!
  users(skip: Int = 0, take: Int = 25): [User!]!
  reply(id: String!): Reply!
}

type Reply {
  id: ID!
  when: Date!
  content: String!
  thread: Thread!
  author: User!
}

type Subscription {
  forumAdded: Forum!
  threadAdded: Thread!
  userAdded: User!
  replyAdded: Reply!
}

type Thread {
  id: ID!
  when: Date!
  title: String!
  content: String!
  forum: Forum!
  author: User!
  replies(take: Int!, skip: Int!): PaginatedReplies!
  lastReply: LastReply!
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
}
