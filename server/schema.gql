# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Forum {
  id: ID!
  name: String!
  threads: [Thread!]!
}

type Mutation {
  addForum(newForumData: NewForumInput!): Forum!
  addThread(newThreadData: NewThreadInput!): Thread!
  addUser(newUserData: NewUserInput!): User!
}

input NewForumInput {
  name: String!
}

input NewThreadInput {
  name: String!
  body: String!
  authorid: Int!
  forumid: Int!
}

input NewUserInput {
  name: String!
  email: String!
  password: String!
}

type Query {
  forum(id: String!): Forum!
  forums(skip: Int = 0, take: Int = 25): [Forum!]!
  thread(id: String!): Thread!
  threads(forumId: String!, skip: Int = 0, take: Int = 25): [Thread!]!
  user(id: String!): User!
  users(skip: Int = 0, take: Int = 25): [User!]!
}

type Subscription {
  forumAdded: Forum!
  threadAdded: Thread!
  userAdded: User!
}

type Thread {
  id: ID!
  when: DateTime!
  title: String!
  forum: Forum!
  author: User!
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
}
